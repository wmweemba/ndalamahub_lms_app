# CLAUDE.md — NdalamaHub

**This is a living document.** It is the single file that explains the whole application — architecture, decisions, current state, and rules of engagement. Update it whenever a meaningful state change happens (a phase from `docs/` is completed, an architecture decision changes, auth is migrated, UI_SPEC lands, etc.). Do not let it drift out of sync with reality — a stale CLAUDE.md is worse than no CLAUDE.md, because it will be trusted.

**Last updated:** 2026-07-22 — Phase 19 (direct lending & borrower identity) executed on `phase/19-direct-lending-identity`, all 5 steps complete, server suite 283/283 (up from 261), client suite unchanged-green at 49. `Company.js` gained `lendingModel: 'employer' | 'direct'` (lender companies only, platform_admin-only to change post-creation); the `tenantScope.js` `companyLenderId()` fail-open for direct borrowers under the subscription gate (found during planning) is fixed — it now resolves a lender-type company to its own id instead of `null`; `/approve`/`/reject` (`routes/loans.js`) give `lender_officer` approval authority over direct loans only (employer-model loans keep the pre-existing rule, enforced by a new `canActOnLoanApproval()` helper) via a new `authorize()` entry; `User.js` now has NRC-first borrower identity (`nrc` required for borrowers, unique per company via a partial index; `email` optional for borrowers via another partial index, replacing the old schema-level `unique: true`; `department` no longer required for borrowers) — see Sections 5 and 7 below for the durable detail, and `changelog.md`'s 2026-07-22 (Phase 19 execution) entry for the full per-step account. A "DirectLend Test" fixture (lender_admin/lender_officer/NRC-only borrower/one pending direct loan) was seeded on the shared dev Atlas cluster for phases 20-24 to verify against.

**Previously (2026-07-22, planning session):** Manifi launch planning session (planning agent, docs only — zero code changes). The demo (2026-07-21) succeeded; Manifi is confirmed as the launch client, target 1 Aug (flexible +1–3 weeks). Clement's requirements and William's decisions are locked in `docs/DECISIONS.md` "Manifi launch decisions (2026-07-22)" — headline items: **direct lending** as lender-level configuration (`lendingModel: 'direct'`, customers attach to the lender company itself; approval matrix gives `lender_officer` approve authority over direct loans; a `companyLenderId` fail-open for direct borrowers under the subscription gate was found during planning and is fixed in Phase 19); **NRC-first borrower identity** (NRC required for all borrowers, unique per company; email optional for borrowers, walk-ins never turned away); **the rollover engine** (25% flat capitalization of outstanding balance after a 14-day grace, new term anchored to the prior due date, indefinitely repeating, stopped only by payment or a manual default — the riskiest build item); **collateral** (verification blocks approval, letter-of-sale flag blocks disbursement, register page, text-only v1); **public website intake** from manifipay.com (prospect records + review/conversion, applications-open-accounts-gated); **auth Option A** — server-side sessions replacing localStorage JWTs (Phase 25; MFA post-launch); **production = Coolify/Hetzner on a fresh DB** (Phase 26; Render/Netlify/Atlas stay permanent dev/demo). Execution docs `docs/19-…` through `docs/26-…` generated (25 and 26 are two-session phases); `docs/README.md` indexed. **Phases 19+ are full-stack — the zero-server-change rule was specific to the UI runway (12–17) and does not apply**; the server suite grows from its 261 baseline instead.

**Previously (2026-07-18):** Netlify production builds were failing outright (`netlify.toml` pinned `NODE_VERSION = "18"`; Vite 7 requires Node 20.19+/22.12+ and refuses to run on anything older, which also broke the `@tailwindcss/oxide` native-binding install as a knock-on effect). Fixed by bumping `NODE_VERSION` to `20.19.0` in `netlify.toml` — see Section 4. No application code changed; this is a deploy-config-only fix, pushed to `main` to trigger Netlify's auto-redeploy.

**Previously (2026-07-17):** Phase 17 (admin surfaces & demo polish) executed on `phase/17-ui-admin-surfaces-demo-polish`, all 5 steps complete including the scripted demo dress rehearsal, merged to `main`. Companies, Settings, Reports, and Support are now all on the design system: `CompaniesPage.jsx` on TanStack Query with tokened table/cards and `CreateCompanyDialog`/`EditCompanyDialog` restyled (payload shapes frozen); `SettingsPage.jsx`'s desktop rail now uses the sage active-pill treatment; `UserManagement.jsx` gained a documented role→pill tint map (role-assignment option-list logic in `CreateUserDialog`/`EditUserDialog` deliberately untouched); `CompanySettings.jsx`/`SystemSettings.jsx` token-restyled only; `SubscriptionManagement.jsx` uses the exact subscription-status tint mapping from the phase doc; `ReportsPage.jsx` lost its debug `console.log`s and JSON `<pre>` dump, moved to TanStack Query and `MetricCard`/`StatusBreakdownCard`, and `ReportModal.jsx` is tokened with `StatusPill`. **Flagged, not a defect**: Reports' card-level "Export" button is functionally identical to "View" (both open the JSON-view modal) — the real PDF/Excel file export already exists and works (confirmed live during the dress rehearsal — a real `.xlsx` downloaded), but lives one level down, on buttons inside `ReportModal` itself, not on the report card. The orphaned `/users` route/page is deleted (confirmed in the production build: falls through to the login redirect). The Step 4 consistency-sweep grep (`bg-blue-\|text-gray-9\|bg-orange-\|shadow-lg`) is clean except two shadcn overlay-primitive hits that are spec-permitted; one straggler outside the phase's named surfaces was fixed anyway (`LoginForm.jsx`'s two leftover `text-gray-400` icon classes). **Step 5 dress rehearsal**: all 9 scripted steps passed end-to-end against the live Atlas dev DB (create borrower → mobile apply → HR approve → disburse/repay/history → arrears pill → all 3 reports incl. a real export download → support ticket round-trip → platform_admin dashboard/Billing) — three pre-existing functional issues found and flagged rather than silently fixed: the borrower no-loans welcome card's empty "Welcome, !" (same root cause as the known JWT-payload/`getCurrentUser()` limitation, CLAUDE.md §6 Phase 06 entry), `Settings → User Management`'s "Add user" form not marking `department` as required client-side despite the server requiring it, and the payment-history dialog's "Total paid" header not matching its own payment rows. `pnpm build && pnpm preview` confirmed the production bundle renders correctly (fonts/brand assets resolve). Client test count unchanged at 49 (no new tests this phase — restyle/cleanup only). All gates green: server 261/261 untouched (zero server diff), client lint/build/test all clean. See `changelog.md`'s 2026-07-17 (Phase 17 execution, Steps 1-4) and (Step 5 — dress rehearsal) entries for full detail.

**Previously (2026-07-17, Phase 16 execution):** Phase 16 (borrower consumer register / mobile) executed on `phase/16-ui-borrower-mobile`, one session, all 5 steps complete, merged to `main`. `BorrowerDashboard.jsx` now has the full consumer-register treatment: a single-column `max-w-md` layout with a hero block (next payment due, or active loan amount when none is due — 34px Plex Mono, warning-tint when due within 7 days), an active-loans mini-card, a most-recent-loan mini-card with `StatusPill`, and an accent "Apply for a loan" CTA card. The loans mobile card list, `LoanDetailsDialog`, and the apply flow (`ProductBasedLoanForm`/`ProductSelector`) were all verified at 375×812 against the live Atlas dev DB and needed no changes — they were already responsive from Phases 14–15. `AppLayout.jsx`'s borrower bottom nav was corrected onto the right tokens (`bg-card`/`text-muted-foreground` instead of sidebar tokens) and gained safe-area padding. `SubscriptionBanner.jsx` moved off the default shadcn `Alert` (generic red `variant="destructive"`) onto the semantic `status-warning`/`status-danger`/`status-info` tint set; `AccountLockedPage.jsx` got a token restyle plus the brand icon. **Flagged, not silently resolved**: `docs/design/mockups/borrower-mobile.html`'s bottom nav shows 4 items (Home/Loans/History/Profile) but `UI_SPEC.md` §10/§16's spec text calls for 3 (Dashboard/Loans/Support, already correct since Phase 12) — per §13's standing rule, spec text wins over the mockup on conflict. Client test count is now 49 (up from 45). All gates green: server 261/261 untouched (zero server diff), client lint/build/test all clean. See `changelog.md`'s 2026-07-17 (Phase 16 execution) entry for full detail.

**Previously (2026-07-17, Phase 15 execution):** loan forms, repayments, products executed on `phase/15-ui-loan-forms-repayments-products`, one session, all 4 steps complete, merged to `main`. `ProductSelector.jsx` is now selectable product cards (sage-tinted on selection) showing rate faithfully via a new `formatRateBasis()` (`src/lib/format.js`) that renders each product's real `interestCalculation.method`/`rateBasis` — Manifi's payday product now reads "25% flat per term", not an assumed "% APR". `ProductBasedLoanForm.jsx`, the four repayment/prepayment/history dialogs (`RecordPaymentDialog`/`PrepaymentDialog`/`PaymentHistoryDialog`/`PrepaymentHistoryDialog`), `ProductsPage.jsx` (now on TanStack Query), and `CreateProductDialog.jsx`/`EditProductDialog.jsx` are all restyled to tokens; `alert()`/silent-`console.error` failure paths are now `sonner` toasts; `ProductsPage`'s product cards use `formatTerm(min/max, termUnit)` instead of a hardcoded "months". **Bug found and fixed during manual verification**: mirroring Phase 14's `getCurrentUser()`-from-localStorage pattern in `ProductBasedLoanForm.jsx` broke the "Full name" field — `POST /auth/login`'s response only ever stores `{id, username, role, name}` (`name` isn't a real `User` schema field) in `localStorage['ndalamahub-user']`, so real logins rendered "undefined undefined"; reverted that one component to fetching `GET /auth/me` (which does return `firstName`/`lastName`) specifically for this field, flagged rather than silently following the established pattern into a regression. Manual verification against the live Atlas dev DB walked both money-movement flows end-to-end: a borrower (`john_employee`) submitted a K2,500/30-day application through the restyled apply flow (toast fired, list updated live with no refresh), and a lender_admin (`manager`) recorded a mobile-money repayment through the restyled `RecordPaymentDialog` on a real active loan (toast fired, installment pill flipped to `paid` live). Client test count was 45 (up from 41). All gates green: server 261/261 untouched (zero server diff), client lint/build/test all clean. See `changelog.md`'s 2026-07-17 (Phase 15 execution) entry for full detail.

**Previously (2026-07-17, Phase 14 execution):** loans list, detail dialog, and lifecycle actions executed on `phase/14-ui-loans-workflow`, one session, all 5 steps complete, merged to `main`. `LoansPage.jsx` and `LoanDetailsDialog.jsx` — the demo centerpiece (approve → disburse) — are now on TanStack Query (`['loans']`, `['companies']`, `['loans', id]`) with `onUpdate` invalidation replacing manual refetch calls; the page no longer makes a `GET /auth/me` round-trip (uses `getCurrentUser()` from `roleUtils` instead). All status rendering goes through `StatusPill` (`getStatusColor` deleted); the `STATUS_PILL_MAP` in `src/components/ui/status-pill.jsx` gained `partial`/`overdue`/`paid` for installment-level statuses (distinct from the loan-level map). The hardcoded `"{term} months"` display is gone — both the list and detail dialog use `formatTerm(loan.term, loan.termUnit)`, so Manifi's 30-day product now reads "30 days" instead of "1 months". Approve/reject/disburse mutations now fire `sonner` toasts alongside the existing inline error display; the trigger buttons for `PaymentHistoryDialog`/`PrepaymentDialog`/`RecordPaymentDialog` were restyled but those dialogs themselves are untouched (Phase 15 scope). Manual verification against the live Atlas dev DB (`manager`/lender_admin) walked the full approve → disburse flow end-to-end on a real loan (LN20260008): approval and disbursement toasts fired, the list updated in place with the correct pill at each stage with no manual refresh, and the `in_arrears`/`overdue` pills were confirmed against a real arrears loan (LN20260009). Client test count was 41 (up from 34). All gates green: server 261/261 untouched (zero server diff — a first parallel Jest run showed spurious `mongodb-memory-server` resource-contention timeouts, resolved by a `--runInBand` rerun), client lint/build/test all clean. See `changelog.md`'s 2026-07-17 (Phase 14 execution) entry for full detail.

**Previously (2026-07-17, Phase 13 execution):** role dashboards executed on `phase/13-ui-dashboards`, one session, all 7 steps complete, merged to `main`. `DashboardPage.jsx` is now a thin role switch rendering one of four new per-role components (`LenderDashboard`, `EmployerDashboard`, `PlatformDashboard`, `BorrowerDashboard`, all in `src/components/dashboard/`), each fetching its own endpoint with TanStack Query instead of the old 1,053-line monolith's `useState`/`useEffect` block. New shared building blocks: `MetricCard.jsx` (replaces `StatCard.jsx`), `StatusBreakdownCard.jsx` (count + pill rows, no progress bars), `src/components/ui/status-pill.jsx` (the canonical UI_SPEC §3.2 status→tint mapping, reused from here on for loan/ticket status everywhere). The Phase 11 arrears counts (`inArrearsLoans`/`defaultedLoans`) are now surfaced: lender dashboard gets an "Overdue" KPI (their sum) plus in-arrears/defaulted breakdown rows; employer dashboard gets the breakdown rows only, no top-level KPI (locked decision, UI_SPEC §11) — `activeLoans` still means strictly `status === 'active'`. The platform dashboard's fake "System Status" card and sample `TrendChart` (which imported `chart.js`, not even a client dependency) are gone with no replacement — no trend endpoint exists, so no chart renders (`StatCard.jsx`/`ProgressBar.jsx`/`TrendChart.jsx` all deleted). Manual verification against the live Atlas dev DB (same seeded accounts as the Phase 12 close-out) confirmed real arrears numbers flow through for both `manager` (lender_admin) and `loan_officer` (lender_officer, still correctly routed to the full LenderDashboard), and caught one layout bug fixed during the same session: the recent-applications table's Amount/Status columns had no horizontal spacing and ran together visually — fixed with `pr-3` cell padding in both `LenderDashboard.jsx` and `EmployerDashboard.jsx`. Client test count was 34 (up from 13). All gates green: server 261/261 untouched (zero server diff), client lint/build/test all clean. See `changelog.md`'s 2026-07-17 (Phase 13 execution) entry for full detail.

**Previously (2026-07-17, Phase 12 close-out):** the Phase 12 manual dev-server spot-check (previously deferred) was closed. Verified against the live Atlas dev DB (`ndalamahub-prod` — see Section 4) using existing seeded accounts (`john_employee`/borrower, `manager`/lender_admin, `loan_officer`/lender_officer, all on password `TestVerify@2026` after a reset — non-production data): (1) borrower sees "Apply for New Loan"/"Apply for Loan" on both the Dashboard and Loans page, (2) lender_admin's Loans-page company filter renders a real company name ("TechCorp Zambia"), not blank/broken, (3) lender_officer lands on the full Lender Portfolio Dashboard with live data (Active Companies/Loans/Users, portfolio breakdown) rather than an empty borrower view, confirming the `355dbb5` `authorizeMinRole('lender_officer')` hotfix on `/api/dashboard/lender-stats` works end-to-end. All three pass. Phase 12 has no further open items.

**Previously (2026-07-16):** Phase 12 (UI foundation & app shell) executed on `phase/12-ui-foundation-shell`, one session, all 10 steps complete, merged to `main`. Fixed the four pre-existing functional bugs `docs/UI_SPEC.md` had flagged (stale `LoansPage.jsx` role keys hiding the borrower Apply button and the lender company filter, wrong `/api/companies` response shape, `lender_officer` falling through to an empty dashboard view) plus a fifth found during execution: `GET /api/dashboard/lender-stats` required `authorizeMinRole('lender_admin')` (level 4) so it 403'd for `lender_officer` (level 3) even though the handler only scopes by `req.user.company` — fixed on `main` directly (outside the UI-phase branch, flagged and approved first) by lowering the gate to `authorizeMinRole('lender_officer')`. Installed self-hosted Inter + IBM Plex Mono; landed the full `UI_SPEC.md` §3 token set in `index.css`; wired the vendored brand assets into `index.html`'s favicon/title. Replaced the 8-block per-route Navbar/SubscriptionBanner duplication in `App.jsx` with one shared `src/components/layout/AppLayout.jsx` (desktop sidebar, mobile top-bar+hamburger for non-borrowers, fixed bottom tab bar for borrowers) — `Navbar.jsx` deleted. Restyled the login page with tokens and the stacked lockup. Added `src/lib/format.js` (`formatCurrency`/`formatDate`/`formatTerm`) for later phases to consume. Deleted 8 verified-dead files; removed Settings' three placeholder tabs (Security/Notifications/Integrations — dead-Save-button stubs) per the locked decision to rebuild them post-demo with real server backing. Stood up the Vitest + RTL harness (`cd client && pnpm test`, 13 tests). Also cleared 23 lint errors that pre-dated this phase on `main` (flagged and fixed on request) — the one substantive fix among them was `LoanDetailsDialog.jsx`'s `handleApprove`/`handleReject`, referenced by the approve/reject buttons but never defined anywhere (a real `ReferenceError` on click, not a style nit); the rest were unused vars/imports, a rules-of-hooks violation in `DashboardPage.jsx` (a redundant pre-hooks early return; a post-hooks guard already covered it), and `vite.config.js`'s ESM `__dirname`. All gates green: server 261/261 unchanged, client lint/build/test all clean. See `changelog.md`'s 2026-07-16 and 2026-07-17 entries for full detail.

**Previously (2026-07-16, planning session):** `docs/UI_SPEC.md` was created, which formally lifts the Section 9 frontend freeze: Part A adopts the NdalamaHub design system that was drafted outside the repo on 2026-07-11 (three registers — light admin with sidebar nav, dark platform control-room, mobile-first borrower; palette anchored on charcoal `#1C1C1C` + magenta-crimson accent `#D6295E`; Inter + IBM Plex Mono), reconciled with the final v2 logo assets (2026-07-12), which are vendored at `client/public/brand/` alongside design references (mockups, palette) at `docs/design/`. Part B is the build spec: per-role screen inventory, demo-critical subset, engineering conventions (zero server changes in UI phases, TanStack Query conversion per phase doc, `lib/format.js` utilities), minimal testing approach (Vitest + RTL from Phase 12), and a deferred-decisions log. Six execution phase docs were generated for the Manifi demo runway — `docs/12-ui-foundation-shell.md` through `docs/17-ui-admin-surfaces-demo-polish.md` (one ~2h Sonnet session each) — plus `docs/18-post-demo-ui-punchlist.md` (backlog, not a session). Key locked decisions: dark platform register is post-demo; arrears presentation = lender "Overdue" KPI + in_arrears/defaulted breakdown rows on both admin dashboards (`activeLoans` stays strictly `active`); `lender_officer` shares the lender dashboard; Settings placeholder tabs (Security/Notifications/Integrations — local-state fakes) are removed until they can be rebuilt with server backing; the orphaned `/users` page is removed in Phase 17; no fake/sample chart data ever (the platform dashboard's hardcoded "sample" trend chart dies in Phase 13); fonts self-hosted via @fontsource.

**Previously (2026-07-11):** Phase 11 (pre-demo cleanup) executed end-to-end on `phase/11-pre-demo-cleanup`, closing out every "flagged, not fixed" code item from Phases 02–10 before the Manifi demo. `server/routes/dashboard.js`'s `/lender-stats` and `/hr-stats` now report `inArrearsLoans`/`defaultedLoans` counts alongside the existing breakdown — the Phase 07 gap where a loan entering `in_arrears` vanished from every portfolio count is closed (`activeLoans` still means strictly `status === 'active'`; presentation is a UI-phase decision). `GET /api/loans` now validates `status` against `Loan.schema.path('status').enumValues`, so both a sanitized-operator-injection attempt (`?status[$ne]=x`, which `express-mongo-sanitize` reduces to `{}`) and a plain typo return a clean 400 instead of a Mongoose cast 500 (the Phase 02 flag). The Phase 04 tenancy-module migration is now complete: `users.js`'s `PATCH /:id/reset-password` reuses the existing `canTouchUser` helper, `reports.js`'s `GET /users` and `products.js`'s `/available`/`/category/:category`/`/stats/overview` now use `tenantScope.js` helpers (`isPlatformAdmin`, `userScopeFilter`, `mergeFilters`, `companyLenderId`) instead of ad-hoc company/role comparisons — `git grep -n "corporateCompanies" server/routes/` now returns nothing, and Section 7's "Not yet migrated" list is gone. New: `POST /api/tickets` now alerts the platform owner on every new ticket (the Phase 08 item deferred out of Phase 09) — email to `OWNER_ALERT_EMAIL` (skipped silently if unset) via a new `emailTemplates.ticketCreated()` template, and a Telegram message via new `server/utils/telegram.js` (`sendTelegramMessage()`, mirrors `email.js`'s never-throws/no-ops-when-unconfigured contract, using global `fetch`); fires for every ticket regardless of handler side (lender-handled and platform-handled alike) — a deliberate single-tenant-era scope decision, revisit once ticket volume makes it noise. Housekeeping: deleted three stray pre-Phase-03b debug scripts (`server/routes/loans.js.backup`, `server/check_user_company.js`, `server/debug_db.js` — all referenced the pre-rename `corporate_hr` role and nothing in live code referenced them); archived three legacy docs that their obsoleting phases had missed (`LOAN_ENGINE_DOCUMENTATION.md`, `FRONTEND_TEST_PLAN.md`, `ZAMBIAN_PAYMENT_COMMUNICATION_GUIDE.md` → `docs/archive/`); removed the unused `JWT_EXPIRE` from the local `server/.env`. Hosting inventory (Section 4) verified with William: backend on **Render** (`https://ndalamahub-lms-app.onrender.com`), frontend on **Netlify** (`https://ndalamahublms.netlify.app/`) — not Vercel, correcting the prior hedge — both pointing at the Atlas `ndalamahub-prod` cluster (the combined dev/staging/demo environment per the 2026-07-11 cutover decision). Suite is now 261/261 (183 model/utility + 78 API-level — 68 + 10 new: 3 in `dashboard.arrears.test.js`, 3 in `loans.filters.test.js`, 2 added to `users.tenancy.test.js`, 2 added to `tickets.test.js`). **Zero client changes**, per the Section 9 frontend freeze. Merged and pushed to `origin/main`.

---

## 1. What NdalamaHub is

NdalamaHub is a multi-tenant Loan Management System (LMS), built and operated by **Nexus** (William's consulting entity, acting as the platform owner). It serves multiple **lender clients** — companies that issue loans — each of which typically operates a payroll-deduction lending model against **employer companies**, whose employees (**borrowers**) receive and repay loans.

**Manifi Investments** is the first confirmed lender client. NdalamaHub is not being built for Manifi specifically — it's being built as a reusable platform that Manifi happens to be the first tenant of. Every architectural decision should hold for a hypothetical second or third lender client. If something only makes sense for Manifi, it belongs in configuration, not in code.

### Hierarchy
```
Nexus (platform owner)
  └── Lender tenants (e.g. Manifi)
        └── Employer companies (a lender's corporate clients)
              └── Borrowers (employees who apply for and repay loans)
```

---

## 2. Current state (as of this writing)

- `feature/phase-0-loan-engine` was **merged into `main`** on 2026-07-04 (clean fast-forward, `main` @ `8ad1f57`). All work now happens against `main`.
- A full pre-go-live audit has been completed and lives at `docs/AUDIT_REPORT.md`. It found several critical issues (see Section 6) that must be fixed before any client-facing use. **Do not treat the app as production-ready until those are resolved.** The audit's original baseline (133 server tests, 111 pass / 22 fail, all failures from the `canAcceptPrepayment` regression) is historical — that regression was fixed in Phase 01, and the suite has been 133/133 since the Phase 01 merge (`0ef9b50`). Phases 02 and 03 are also complete. See `docs/AUDIT_REPORT.md` for the original pre-fix figures.
- Locked decisions from the planning session live at `docs/DECISIONS.md`. Treat that file as authoritative — this CLAUDE.md summarizes it, but `DECISIONS.md` is the source of truth if they ever disagree (and if they do, that's a bug in this file — fix it).
- Phased execution plans **have been generated**: `docs/01-security-critical-fixes.md` through `docs/11-pre-demo-cleanup.md`, indexed in `docs/README.md`, plus `docs/03b-api-test-scaffold.md` (an API/route-level test scaffold and tenant-isolation regression baseline, inserted between 03 and 04). Phases 01–03, 03b, and 04–11 are all executed. Claude Code executing a phase must follow that phase's document exactly — see Section 11 (Working Rules) before touching code.
- Pre-existing roadmap/planning documents (PRODUCTION_ROADMAP, WEEK_* summaries, etc.) were archived to `docs/archive/` and must not be used as planning input.
- **Manifi launch phase docs generated 2026-07-22**: `docs/19-direct-lending-and-borrower-identity.md` through `docs/26-coolify-production-launch.md` (direct lending, rollover engine, collateral, public intake + review queue, customers surface, auth sessions migration, Coolify production launch), driven by `docs/DECISIONS.md` "Manifi launch decisions (2026-07-22)". None executed yet. These are **full-stack** phases — server changes are in scope again, with the growing API test suite as the contract.
- **UI build phase docs generated 2026-07-16**: `docs/UI_SPEC.md` (the UI specification — see Section 9) plus execution docs `docs/12-ui-foundation-shell.md` through `docs/17-ui-admin-surfaces-demo-polish.md` (the six-session Manifi demo runway) and `docs/18-post-demo-ui-punchlist.md` (post-demo backlog, not a session). **Phases 12 through 17 are all executed and merged to `main`** (2026-07-16, 2026-07-17, 2026-07-17, 2026-07-17, 2026-07-17, and 2026-07-17 respectively) — the six-session Manifi demo UI runway is complete. `docs/18-post-demo-ui-punchlist.md` is the only remaining doc, and it's a backlog, not a session. Brand assets are vendored at `client/public/brand/`; design references (register mockups, palette) at `docs/design/`.

---

## 3. Tech stack

- **Monorepo**: root / `client` / `server`, pnpm workspaces-style (not formal pnpm workspaces — root `package.json` just runs `client` and `server` concurrently via `concurrently`)
- **Package manager**: pnpm (`pnpm@10.12.1` pinned in `packageManager` field — respect this, don't use npm/yarn)
- **Backend**: Node.js, Express 4.21.2 (do not upgrade to Express 5.x — this was previously tried and reverted due to a `path-to-regexp` compatibility issue), Mongoose 8.x, MongoDB (currently Atlas, migrating — see Section 4)
- **Frontend**: React 19, Vite 7, Tailwind CSS 4, shadcn/ui, TanStack Query, React Hook Form + Zod, React Router 7
- **Auth**: JWT (`jsonwebtoken`, `bcryptjs`) — see Section 8, this is planned to change
- **Testing**: Jest 30 on the server, 283 tests total as of Phase 19 (up from the 261 Phase 11 baseline — 22 new API-level tests: `server/tests/api/directLending.test.js` and `server/tests/api/borrowerIdentity.test.js`, plus 4 added to `companies.tenancy.test.js`, covering the approval matrix, the `companyLenderId` fail-open fix, and NRC-first borrower identity). 183 are model/utility tests under `server/utils/__tests__/` (133 pre-existing model tests, 17 for `server/utils/tenantScope.js` added in Phase 04, 20 added in Phase 05 for `annualizeRate`/`termToDays`, the Manifi rate-basis/term-unit shape, waived-settlement bookkeeping, and the accrued-interest fix, 3 added in Phase 06 for the user-deletion policy and the orphan-applicant populate guard, 6 added in Phase 07 for `markOverdueInstallments()` (overdue/in_arrears/defaulted transitions, idempotency, recovery, waived/paid non-transition), and 4 added in Phase 09 for `sendPaymentReminders()` (T-3 window selection, reminderSentAt/overdueNoticeSentAt idempotency) — instantiate models/call the scoping module directly, using `mongodb-memory-server` only where a real DB lookup is needed). Since Phase 03b, Supertest and `mongodb-memory-server` are also exercised by 78 API-level tests under `server/tests/api/`, which hit the live Express app (`server/app.js`) over HTTP against a real (in-memory) MongoDB — these exercise route handlers, `authorize`/`authorizeMinRole` chains, and per-route tenancy checks, including 10 added in Phase 08 (`tickets.test.js`) for handler-company routing, the visibility matrix, status-change authority, and the reply-reopens-a-resolved-ticket behavior, 3 added in Phase 09 (`email.test.js`) pinning the no-token-in-forgot-password-response contract and the approve-route email wiring (with `utils/email.js` mocked — no real sends in the automated suite), 10 added in Phase 10 (`subscriptionGate.test.js`) for the exempt-prefix matrix, tenant inheritance, the read-only GET/write split, a stale-trialing-locks-without-a-sweep-run check, and sweep idempotency, and 10 added in Phase 11: 3 in `dashboard.arrears.test.js` (arrears counts flow through the real `markOverdueInstallments()` job into both dashboards, with tenancy holding on the new fields), 3 in `loans.filters.test.js` (the `status` enum-validation 400s), 2 added to `users.tenancy.test.js` (reset-password cross-tenant deny/allow), and 2 added to `tickets.test.js` (owner-alert-fires / alert-unconfigured-still-succeeds, with `utils/email.js` and `utils/telegram.js` mocked). Note: Phase 08's doc specified the ticket tests' path as `server/utils/__tests__/tickets.test.js`, and Phase 10's doc specified `server/utils/__tests__/subscriptionGate.test.js`, but since both are Supertest/HTTP tests (not unit tests calling models directly), they were placed under `server/tests/api/` instead, consistent with the Phase 03b convention — a deliberate deviation from each doc, not an oversight. A `test.failing` entry (none currently exist) marks a known, deliberately-unfixed defect the test suite pins rather than hides. **Client-side**: Vitest + React Testing Library, stood up in Phase 12 (`cd client && pnpm test` = `vitest run`, 49 tests as of Phase 16 — `src/lib/format.test.js` and `src/components/layout/AppLayout.test.jsx` from Phase 12; `src/components/ui/status-pill.test.jsx`, `src/components/dashboard/{LenderDashboard,EmployerDashboard,PlatformDashboard}.test.jsx`, and `src/pages/dashboard/DashboardPage.test.jsx` from Phase 13; `src/pages/loans/LoansPage.test.jsx` and `src/components/loans/LoanDetailsDialog.test.jsx` from Phase 14; `src/pages/products/ProductsPage.test.jsx`, `src/components/loans/ProductBasedLoanForm.test.jsx`, and `src/components/loans/RecordPaymentDialog.test.jsx` from Phase 15; `src/components/dashboard/BorrowerDashboard.test.jsx`, `src/components/layout/SubscriptionBanner.test.jsx`, and a bottom-nav case added to `AppLayout.test.jsx` from Phase 16; see UI_SPEC §14 for the deliberately-minimal scope through the demo runway).
- **Scheduled jobs**: `server/jobs/scheduler.js` runs three daily cron jobs via `node-cron` (Africa/Lusaka), started from `server/server.js` unless `NODE_ENV=test` or `ENABLE_CRON=false`: `markOverdueInstallments()` (`server/jobs/markOverdueInstallments.js`, built Phase 07) at 01:00, `expireSubscriptions()` (`server/jobs/expireSubscriptions.js`, built Phase 10 — advances the persisted subscription status through its four transitions and emails affected lender admins; enforcement itself never depends on this job having run, see Section 6) at 02:00, and `sendPaymentReminders()` (`server/jobs/sendPaymentReminders.js`, built Phase 09 — T-3-day reminders and overdue notices, both idempotent via `reminderSentAt`/`overdueNoticeSentAt` stamps on the installment) at 08:00. Manual/operator triggers: `pnpm job:overdue` (`server/jobs/runMarkOverdue.js`), `pnpm job:subscriptions` (`server/jobs/runExpireSubscriptions.js`), and `pnpm job:reminders` (`server/jobs/runSendReminders.js`) — also how the first production backfill/send happens post-deploy. Single-process assumption only; if the Coolify migration ever runs more than one server instance, these jobs need a lock to avoid double-running.
- **Email & notifications**: `server/utils/email.js` (`sendEmail`) via Resend — built Phase 09. Never throws; no-ops with a warn log when `RESEND_API_KEY` is unset, so dev/test never need credentials. Templates in `server/utils/emailTemplates.js`, including `subscriptionNotice()` (Phase 10) and `ticketCreated()` (Phase 11). `server/utils/telegram.js` (`sendTelegramMessage`) — built Phase 11, mirrors `email.js`'s contract exactly (never throws, no-ops with a warn log when `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` are unset), uses global `fetch`. See the Phase 09 entry (in the git history — see `changelog.md`) and the Phase 11 entry in the Last-updated block for what's wired.
- **Exports**: PDFKit, ExcelJS — confirmed working end-to-end (not placeholders)
- **Billing**: manual (no payment processor) — see Section 2's Phase 10 entry and Section 6.

### Test command — resolved
The placeholder-test-script concern was based on a stale pre-merge snapshot. Post-merge, `server/package.json` has `"test": "jest"` (plus `test:watch`, `test:coverage`), and `cd server && pnpm test` was verified to run the real test suite on 2026-07-04 (133 tests then; 261 as of Phase 11). Treat `pnpm test` in `server/` as the regression gate. Since Phase 03b, `server.js` is a thin bootstrap (dotenv/env fail-fast/`connectDB()`/`app.listen()`); the actual Express app lives in `server/app.js` and is what the API tests import — `pnpm start`/`pnpm dev` behavior is unchanged.

---

## 4. Environments & deployment

- **Current**: verified with William 2026-07-11 (Phase 11). Backend on **Render** (`https://ndalamahub-lms-app.onrender.com`); frontend on **Netlify** (`https://ndalamahublms.netlify.app/`) — not Vercel, correcting the prior hedge. Both point at the Atlas `ndalamahub-prod` cluster, the combined dev/staging/demo environment (see below).
- **Netlify build config** (`netlify.toml`, repo root): `[build.environment] NODE_VERSION` must stay at `20.19.0` or higher — Vite 7 (and the `@tailwindcss/oxide` native binding it loads via `@tailwindcss/vite`) hard-requires Node 20.19+/22.12+ and fails the build outright on anything older (hit in production 2026-07-18: Netlify's build image was pinned to Node 18, which failed both the Vite version gate and, as a knock-on effect, `@tailwindcss/oxide`'s platform-specific optional-dependency install). Don't lower this pin without checking Vite's current minimum first.
- **Target**: migrating to a Coolify-managed Hetzner VPS, using a MongoDB service already running on Coolify. This migration is expected once the audit-fix phases are complete — not before. **Production starts on a fresh, empty database — no data is migrated** (locked decision, 2026-07-11, `docs/DECISIONS.md` "Production cutover"): everything in Atlas is dummy/verification data, so the per-phase deploy notes about counters seeds, role migrations, and backfills do not apply to the production cutover. **The current Atlas cluster (`ndalamahub-prod`) and dev environment remain in service permanently as the combined dev/staging/demo environment** (confirmed by William, 2026-07-10 and 2026-07-11) — client demo sessions run against it, and it is not decommissioned. Atlas remains a legitimate target for manual phase-verification writes (seeded test data, one-off scripted checks) without needing to ask each time; just don't treat anything in it as real user data.
- No secrets, connection strings, or credentials belong in this file, in commit messages, or in any doc committed to the repo. Reference environment variable **names** only (e.g. `MONGODB_URI`, `JWT_SECRET`). See `docs/AUDIT_REPORT.md` Section 9 for the current inventory of env vars actually used vs. documented.
- `server/.env.example` and `client/.env.example` exist (added in Phase 02) — names and placeholder comments only, no real values. Server required vars: `MONGODB_URI`, `JWT_SECRET` (server exits at boot if either is missing); optional: `PORT`, `NODE_ENV`, `CORS_ORIGIN`, `ENABLE_CRON`, (Phase 09) `RESEND_API_KEY`, `FROM_EMAIL`, `FROM_NAME`, `APP_URL` — all four email vars are optional; without `RESEND_API_KEY` the app boots and runs fully, mail sending just no-ops with a warn log — and (Phase 11) `OWNER_ALERT_EMAIL`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, also all optional and silently no-op when unset. Client: `VITE_API_URL`.

---

## 5. Roles

Roles were renamed and reordered in Phase 03 (2026-07-09) — this table is current reality across schema, middleware, routes, client, and the database (migrated via `server/utils/migrations/renameRoles.js`).

| Role | Level | Scope |
|---|---|---|
| `platform_admin` | Platform (5) | Nexus-level. Manages all lender tenants and platform-wide settings. |
| `lender_admin` | Lender tenant (4) | Manages one lender's operation: products, staff, employer clients, disbursement. |
| `lender_officer` | Lender tenant (3) | Loan officer. Processes applications, records repayments, manages assigned portfolios. |
| `employer_admin` | Employer client (2) | Runs an employer account — manages that employer's users and settings. |
| `employer_hr` | Employer client (1) | Reviews/approves employee loan applications. Read-only on repayment data. |
| `borrower` | Individual (0) | Employee applying for and repaying loans. |

The hierarchy is a deliberate reorder from the original planning-era numbering — lender roles sit strictly above employer roles (locked decision, `docs/DECISIONS.md`, "Role hierarchy correction"). The old names (`super_user`, `corporate_admin`, `corporate_hr`, `corporate_user`, `lender_user`) and the two ghost roles (`client_admin`, `staff`) no longer exist anywhere in code or data — do not reintroduce them.

**Repayment recording authority**: `lender_admin` and `lender_officer` only. Employer-side roles are read-only on repayment/schedule data — this is enforced server-side (route guards + tenancy checks in `server/routes/loans.js`), not just hidden in the UI.

**Loan approval authority (Phase 19, direct lending)**: `PUT /api/loans/:id/approve` and `/reject` gate approval differently depending on the loan's shape, via `canActOnLoanApproval()` in `server/routes/loans.js`. A loan is **direct** when its `company` and `lenderCompany` are the same document (the borrower attached straight to the lender, no employer in between — see the `lendingModel` note in Section 7). For a direct loan, either `lender_officer` or `lender_admin` of that lender may approve/reject; for an employer-model loan, the rule is unchanged from before this phase (employer-side of the loan's company, or the loan's `lender_admin` — `lender_officer` has no authority over employer-model loans). `/disburse` stays `lender_admin`-only regardless of loan shape.

**Borrower identity (Phase 19, NRC-first)**: every `borrower` requires an `nrc` (`server/models/User.js`), unique per company (not globally — the same person may legitimately exist under two lender tenants) via a partial index on `{ company, nrc }`. `email` is optional for borrowers (required for all other roles) via its own partial unique index — a borrower with no email logs in by username as usual. `department` is no longer required for borrowers (still required for `employer_hr`).

---

## 6. Known critical issues (from `docs/AUDIT_REPORT.md`)

These are non-negotiable, phase-1 fixes — not up for reprioritization against feature work:

- ~~Public registration accepts an attacker-chosen role, including the top-level admin role (total RBAC bypass)~~ — **Fixed in Phase 01** (`server/routes/auth.js`: `/register` route removed; `server/routes/users.js`: lender-admin role-assignment guard added)
- ~~Repayment recording endpoint is dead (throws on every call)~~ — **Fixed in Phase 01** (`server/routes/loans.js`: validation block moved inside `try`, cross-tenant write hole closed via `loan.lenderCompany` check)
- ~~Prepayment/early-settlement engine is dead (regression from commit `531d954`)~~ — **Fixed in Phase 01**: `Loan.canAcceptPrepayment()` return statement restored, the three schedule-recalculator argument-order bugs fixed, and (via Addendum A) `calculateEarlySettlementAmount()`'s savings formula corrected to count all unpaid installments' interest, not just future-dated ones. Suite is 133/133.
- ~~Several admin user-management endpoints 500 due to a non-existent method being called on the JWT payload~~ — **Fixed in Phase 01** (`server/middleware/auth.js`: added `hasMinRole` helper; `server/routes/users.js`: replaced 5 `req.user.hasPermission()` call sites)
- ~~Two ghost roles (`client_admin`, `staff`) referenced in dead code paths that don't exist in the schema~~ — **Fixed in Phase 03**: `client_admin` sites in `server/routes/loans.js` (approve/reject) and `server/routes/reports.js` (four lender-portfolio scoping branches plus the `GET /reports/companies` `authorizeMinRole` gate, which had resolved to level 0 — a broken open gate — and now requires `lender_admin`) replaced with the correct real role; `staff` was already removed with the register route in Phase 01
- ~~No cron/scheduler exists — arrears/overdue status never triggers, ever~~ — **Fixed in Phase 07**: see the new entry below
- ~~Hardcoded JWT fallback secret, unused rate limiter, inconsistent token payload shapes breaking refresh~~ — **Fixed in Phase 02**: `server/utils/auth.js` and the `/refresh` route no longer fall back to `'your-secret-key'` (server now fails fast at boot if `JWT_SECRET`/`MONGODB_URI` are unset); access/refresh tokens now share a canonical `{id, username, role, company}` payload, fixing the previously-unusable refresh flow; the existing per-username login rate limiter is wired up (5/15min) alongside a broader `express-rate-limit` backstop on `/api/auth` (50/15min)
- ~~No `helmet`, `express-rate-limit`, or `express-mongo-sanitize`~~ — **Fixed in Phase 02**: all three added to `server/server.js`; `express-mongo-sanitize` closes the `?status[$ne]=x`-style operator-injection class on list endpoints
- ~~14 high-severity vulnerable server dependencies, 26 on the client~~ — **Server fixed in Phase 02**: `pnpm audit --prod` on the server now shows 0 vulnerabilities (via `pnpm update` + a `pnpm.overrides` pin on transitive `uuid`). Client: `axios`/`react-router-dom`/`vite` bumped within current majors, but several high/moderate findings remain — all transitive dev-build-tool dependencies (`rollup`, `picomatch`, `postcss`, `esbuild` via `@tailwindcss/vite`→`vite`, plus `form-data` via `axios`) not yet resolved; see `changelog.md` Phase 02 entry

Full detail, evidence, and file/line references are in `docs/AUDIT_REPORT.md` — don't duplicate that detail here, just be aware it exists and gates everything else. Phase 01 (register/RBAC/repayment/prepayment fixes plus the settlement-savings fix) and Phase 02 (auth/platform hardening, all items above) are both merged into `main`. All security-baseline items from the original audit are now resolved.

**Found during Phase 03b planning and hotfixed same day (2026-07-10, directly on `main` at William's direction, outside a numbered phase):** two critical authorization defects neither the audit nor phases 01–03 had caught: (1) `PUT /api/loans/:id/approve` and `/reject` skipped the tenancy check entirely for `lender_admin` callers, letting a lender admin of one tenant approve/reject another lender's pending loans — now requires `loan.lenderCompany` to match the caller's company (fail-closed if `lenderCompany` is missing); (2) `PUT /api/users/:id` had no role-escalation cap on its role-update branch (unlike `POST /api/users`, which got one in Phase 03), letting an `employer_admin` promote a same-company user to `lender_admin` or `platform_admin` — now enforces the same `hasMinRole(caller, targetRole)` rule with a 403. Loan *reads* were never affected (list/detail/summary/export already checked `lenderCompany`). Phase 03b added API regression tests pinning both fixes (`server/tests/api/hotfix-regressions.test.js`); Phase 04's route migration preserved these semantics (verified green throughout).

**Closed in Phase 04 (multi-tenancy scoping layer, 2026-07-10):** several tenant-isolation gaps the audit had flagged as part of the "no middleware, ad-hoc checks" problem are now fixed as a side effect of the migration onto `server/utils/tenantScope.js` — the lender-admin list-filter `$or` overwrite (search silently broken for lender admins), the always-zero dashboard company/user counts (`dashboard.js` `/stats` was building a bogus `{company: req.user.company}` filter against the `Company` model, which has no `company` field), cross-tenant probing of another lender's product via `POST /:id/check-eligibility`, `/calculate-fees`, `/calculate-schedule`, the global (unscoped) counts on `GET /api/system/info`, and the legacy-loan `lenderCompany` null-crash on `GET /:id/summary`. See Section 7.

~~Left unmigrated in Phase 04: `PATCH /api/users/:id/reset-password`, `GET /api/reports/users`, and three `products.js` routes (`/available`, `/category/:category`, `/stats/overview`) still use the pre-Phase-04 ad-hoc company/role comparison pattern~~ — **Fixed in Phase 11**: all four routes now use `tenantScope.js` helpers (`canTouchUser`, `userScopeFilter`/`mergeFilters`, `isPlatformAdmin`/`companyLenderId`). See Section 7.

~~The `express-mongo-sanitize` fix correctly strips injected Mongo operators (e.g. `$ne`) from query params, but on `GET /api/loans` this leaves `filter.status` as an empty object, which Mongoose then fails to cast, producing a 500 instead of a graceful filtered/empty result~~ — **Fixed in Phase 11**: `GET /api/loans` now validates `status` against `Loan.schema.path('status').enumValues` and returns 400 for anything that isn't a real status string (covers both the sanitized-operator case and plain typos).

**Closed in Phase 05 (loan rate engine rebuild, 2026-07-10):** the implausible ~21 million prepayment-summary interest figure flagged during Phase 01 Step 8 verification is fixed — root cause was `Loan.calculateAccruedInterest()` summing interest from every `paid` installment ever recorded (not just outstanding ones), which inflated both the early-settlement quote and the prepayment `beforeInterest`/`afterInterest` figures on any loan with payment history. Now only counts interest on installments that are still outstanding (not `paid`, not `waived`) and past due. Also closed in this phase: schedules were anchored to application date instead of disbursement date (`server/routes/loans.js` disburse route now calls `calculateLoanDetails()` to regenerate the schedule against `disbursedAt`, and sets `endDate` to the last installment's real due date instead of a `term × 30 days` approximation); early-settled loans marked remaining installments `paid` with `paidAmount: 0`, corrupting `getSummary()` totals (now marked `waived`, with the settlement amount counted once via `loan.earlySettlement.settlementAmount`); interest rate was hardcoded as annualized and term as months-only, which could not represent Manifi's confirmed 25%-flat-rate/30-day-term product without day-count drift (rate basis and term unit are now configurable per product/loan — see Section 7). Loan-number generation also moved from a `countDocuments`-based scheme (race-prone under concurrent creates) to an atomic `counters` collection increment — **deploy note:** before this code goes live anywhere with existing loan data, seed that environment's `counters` collection for the current year to the current max sequence, e.g. `db.counters.updateOne({_id: "loanNumber-2026"}, {$set: {seq: <max existing LN2026#### suffix>}}, {upsert: true})`, or new loan numbers will collide with existing ones.

**Closed in Phase 06 (user deletion policy & orphan safety, 2026-07-11):** `DELETE /api/users/:id` (`server/routes/users.js`) previously hard-deleted unconditionally for any caller with `employer_hr`-or-above access, contradicting the locked decision that users with any loan history are never hard-deleted. It now checks `Loan.exists({ applicant: id })` first — if true, the user is deactivated (`isActive: false`) instead, for every caller role including `platform_admin`; only users with zero loan history are hard-deleted. Login (`server/routes/auth.js`) was also found not to check `isActive` at all, meaning soft-delete had no actual effect on a deactivated user's ability to log in — fixed by rejecting deactivated users with the same generic "Invalid username or password" message used for other login failures. Separately, orphaned loans from *past* hard-deletes (predating this fix) were already corrupting reads: `GET /api/dashboard/hr-stats` 500'd on any populated `loan.applicant` that no longer resolved, its `employeesWithLoans` dedupe count was also wrong even without orphans (stringifying the whole populated document instead of just the id), and the CSV/report export in `server/routes/reports.js` had the same crash risk. All now guard with `loan.applicant ? ... : 'Deleted user'` (matching the pre-existing pattern already used in `/reports/overview`). `dashboard.js` `/user-stats`'s `nextPaymentDue` was also fixed to take the earliest pending-installment due date across *all* the caller's active loans, not just the first active loan found. **Known limitation, accepted for now:** a deactivated user's outstanding JWT remains valid until it expires (up to 7 days) — `authenticateToken` doesn't hit the DB per request. The planned auth migration (Section 8) is expected to address this; a per-request `isActive` lookup would be the stopgap if unacceptable before then. Nine pre-existing orphaned loans were found on the Atlas dev database during verification (listed, not repaired, per this phase's explicit scope) — see `changelog.md` Phase 06 entry for the loan numbers.

**Closed in Phase 07 (arrears & cron infrastructure, 2026-07-11):** the platform's first scheduled job now exists. `server/jobs/markOverdueInstallments.js` finds loans (`active`/`in_arrears`) with `pending`/`partial` installments past due, flips those installments to `overdue`, and saves each loan (the pre-save hook's `updatePaymentTracking()`/`checkArrearsStatus()` then handle the status transition) — sequential saves by design, not `updateMany`, so the model hooks stay in the loop. `server/jobs/scheduler.js` runs it daily at 01:00 Africa/Lusaka via `node-cron`, started from `server/server.js` unless `NODE_ENV=test` or `ENABLE_CRON=false`; `pnpm job:overdue` (`server/jobs/runMarkOverdue.js`) is the manual/operator trigger, also used for the first production backfill after deploy. `Loan.checkArrearsStatus()` (previously escalate-only) now also recovers `in_arrears` → `active` once no installments remain overdue (`defaulted` never auto-recovers — reversing a default is a human decision); `calculateDaysInArrears()`'s latent bug — using `.find()` (array-order-dependent) instead of the earliest-due overdue installment — was also fixed. ~~**Flagged, not fixed (out of this phase's scope):** verification found `server/routes/dashboard.js`'s `/lender-stats` and `/hr-stats` have no `in_arrears`/`defaulted`/overdue counters at all — a loan entering `in_arrears` simply vanishes from every dashboard count~~ — **Fixed in Phase 11**: both routes now report `inArrearsLoans`/`defaultedLoans` counts (`activeLoans` still means strictly `status === 'active'`; presentation is a UI-phase decision). **Deploy note:** run `pnpm job:overdue` once, manually, immediately after this code goes live in any environment with existing loan data, to backfill arrears status for installments that are already overdue.

---

## 7. Multi-tenancy

Built in Phase 04 (2026-07-10). Shared-database multi-tenancy by company ObjectId (not per-tenant deployment); no `TENANT_ID` env-var pattern. Onboarding a second or third lender client is just inserting a `Company` document — nothing in this layer changes.

**`server/utils/tenantScope.js`** is the single reusable scoping module. Route handlers keep their `authorize`/`authorizeMinRole` guards for *who may call this route*; the module answers *which documents that caller may see or touch*:

- `loanScopeFilter(user)` / `userScopeFilter(user)` / `companyScopeFilter(user)` — Mongo filter fragments for list queries, scoped by role (platform admin → `{}`; lender-side → their company + client companies; employer-side → their company; borrower → their own records).
- `mergeFilters(...fragments)` — combines filter fragments via `$and`, so a route's own criteria (e.g. a search `$or`) never collides with the scope filter's `$or`. This is the fix for the pre-Phase-04 bug where a lender admin's scope filter silently overwrote their search filter on `GET /api/loans`.
- `canReadLoan(user, loan)` / `canWriteRepayment(user, loan)` / `canReadCompany(user, company)` / `canReadProduct(user, product)` — document-level access checks. `canWriteRepayment` is lender-side-only (repayment/prepayment/settlement recording authority, per Section 5). Missing refs (e.g. `loan.lenderCompany` on a legacy loan) fail closed (`false`), never throw.
- `clientCompanyIds(lenderCompanyId)` / `companyLenderId(companyId)` — helpers resolving the lender↔employer relationship in either direction. **Since Phase 19**, `companyLenderId()` resolves a lender-type company to its own `_id` (previously returned `null` for lender companies, which fail-opened the subscription gate for direct-model borrowers — see below — and broke product resolution for them; employer-type companies still resolve to their `lenderCompany` field as before).

Migrated onto this module: `server/routes/{loans,reports,dashboard,users,companies,products,system}.js`. `users.js` additionally has a local `canTouchUser(reqUser, targetUser)` helper (platform admin, same company, or lender-side with target in a client company) for its per-user document checks, also used since Phase 11 by `PATCH /:id/reset-password`. `products.js` has a local `canAccessLenderProduct(user, product)` wrapping `canReadProduct` to resolve the employer-side/borrower case via `companyLenderId` (the eligibility/fees/schedule probe endpoints, plus — since Phase 11 — `/available`, `/category/:category`, and `/stats/overview`, all now tenant-checked instead of ad hoc). `reports.js`'s `GET /users` was migrated onto `userScopeFilter`/`mergeFilters` in the same phase. Every route originally flagged in Section 6 as "left unmigrated" is now on this module — there is no remaining ad-hoc-pattern list.

New ad-hoc company-ObjectId comparisons should not be added anywhere in `server/routes/` going forward — use the module.

**Billing/subscription gating** (built Phase 10, `server/middleware/subscription.js`) composes with this layer rather than replacing it: `companyLenderId` is how an employer/borrower's *paying lender* is resolved for gating purposes, exactly as it's used here for read/write scoping. Tenancy answers "which documents"; the subscription gate answers "is this tenant's business access currently on at all" — see the Phase 10 entry in Section 2 for the full lifecycle.

**Direct lending (Phase 19)**: a lender `Company` document now has `lendingModel: 'employer' | 'direct'` (default `'employer'`; meaningless on corporate companies, schema-rejected if set to `'direct'` there). Under the direct model, a borrower's `company` field points straight at the lender company itself — no employer/corporate company in between — and loan creation (`POST /api/loans`) sets that loan's `company` and `lenderCompany` to the same lender id. `routes/users.js`'s `POST /` lets `lender_admin`/`lender_officer` attach a `borrower` to their own company only when that company's `lendingModel === 'direct'`; employer-model lenders keep the pre-existing rule (borrowers belong to client companies only). Only `platform_admin` may change an existing lender's `lendingModel` (`PUT /api/companies/:id` strips the field for `lender_admin` callers).

---

## 8. Auth

**Current**: custom JWT implementation (`jsonwebtoken` + `bcryptjs`). As of Phase 02, the known critical issues from the security baseline (Section 6) are fixed: no hardcoded fallback secret, fail-fast on missing `JWT_SECRET`/`MONGODB_URI`, unified `{id, username, role, company}` token payload shared by login and refresh (refresh flow now actually works), login rate limiting wired up, and `helmet`/`express-rate-limit`/`express-mongo-sanitize` in place. Login now also returns a `refreshToken` — additive, since the client doesn't yet call `/api/auth/refresh` (still using a single long-lived access token). Tokens remain in `localStorage` (XSS-exfiltratable) and access tokens remain 7-day-lived; both are deliberately unchanged pending the auth migration below.

**Decided (2026-07-22), executes in Phase 25**: migration to **server-side sessions** (express-session + Mongo session store; httpOnly/secure/SameSite-Lax cookies; per-request user load giving instant deactivation lockout; Origin-check CSRF posture; JWT issuance/refresh and `JWT_SECRET` retired). Staff TOTP MFA is deliberately post-launch. See `docs/25-auth-sessions-migration.md` and DECISIONS.md item 7. **Do not invest in refactors of the current JWT internals** — Phase 25 removes them. Rewrite this section to describe the session design once Phase 25 merges.

---

## 9. Frontend rules

**The design system is live** (adopted 2026-07-16, fully built out as of 2026-07-17): `docs/UI_SPEC.md` is the source of truth for all visual/UX decisions on this app — design system (Part A: three registers, palette, typography, component rules) and build spec (Part B: screen inventory, demo scope, conventions, testing approach). All UI changes must conform to it. The six-phase Manifi demo runway (`docs/12-…` through `docs/17-…`) is complete and merged: tokens, fonts, brand assets, the shared `AppLayout`, login, dashboards, loans workflow, loan forms/repayments/products, the borrower consumer register, and the admin surfaces (Companies/Settings/Reports/Support) are all on the design system. `docs/18-post-demo-ui-punchlist.md` is the only remaining doc — a backlog, not a session; see UI_SPEC §12 for what's deliberately deferred (dark platform-admin register, Settings placeholder tabs, auth screens beyond the login restyle, charts, `window.confirm`→styled dialogs, RHF+Zod adoption). Rules for frontend work going forward:

- Any new UI work (post-demo punch list items, new features) must conform to `docs/UI_SPEC.md` — treat it as living style-guide, not a one-time migration target. If a change would contradict it, stop and flag rather than drift.
- **UI-only changes still make zero server changes** as a matter of habit from the demo runway, though this is no longer a hard phase-doc gate now that the runway is done — `git diff main -- server/` should still come back empty for a change that only touches presentation. A UI change that turns out to need a server change stops and flags it rather than reaching into `server/` unplanned.
- Client gates: `cd client && pnpm lint && pnpm build && pnpm test` (Vitest + React Testing Library, deliberately minimal — see UI_SPEC §14). Server suite (261 tests) stays untouched by UI-only work.
- The dark platform-admin register, the removed Settings placeholder tabs, auth screens, and charts are all deliberately post-demo — see UI_SPEC §12 before "improving" any of them.

---

## 10. Coding conventions

No formal style guide is being imposed yet — conventions may still shift (auth migration, tenancy layer, UI_SPEC), so locking in patterns now risks having to unwind them later. For now:

- Infer conventions from the existing codebase where they're consistent, and flag inconsistencies rather than silently picking one
- Respect the existing ESLint config on the client (`client/eslint.config.js`) — there's no server-side lint config yet; don't add one without discussing it first
- When a phase doc specifies an exact approach (function names, file structure, etc.), follow it exactly rather than substituting a "better" pattern — see Section 9 of the workflow rules below
- Prefer root-cause explanation and a proposed fix before making changes, rather than jumping straight to a patch — this applies to all work on this repo, not just security fixes

---

## 11. Working rules (Fable / Sonnet workflow)

This project uses a two-agent workflow:

- **Claude Code Fable** is the planning agent. It audits, investigates, and produces numbered phase documents in `docs/` (e.g. `docs/01-security-baseline.md`). It does not write production code.
- **Claude Code Sonnet** is the execution agent. It follows a phase document **exactly as written**, with no improvisation authority. If a phase document is ambiguous, incomplete, or appears wrong once execution starts, Sonnet should stop and flag it rather than filling the gap with its own judgment.

If you are Claude Code operating in this repo and there is no specific phase document driving the current task, ask which phase document to follow, or whether this is genuinely a new, undocumented task — don't assume free rein just because CLAUDE.md exists.

Every phase document is expected to be sized to fit a single ~1.5–2 hour focused session, with explicit stopping/resume points if it can't be. Don't try to compress multiple phases into one sitting even if it seems achievable — the phase boundaries exist on purpose.

The test suite (261 server tests as of Phase 11 — see Section 3 for the model/utility vs. API-level breakdown; `docs/AUDIT_REPORT.md` Section 10 records the original pre-fix baseline of 111 passing, since fixed in Phase 01) is the standing regression check. Treat "suite is green" as a gate before considering any phase complete, once the test-command issue in Section 3 is resolved. From Phase 04 onward, `server/tests/api/hotfix-regressions.test.js` specifically is the regression gate for the two 2026-07-10 authorization fixes — any route migration must keep it green.

---

## 12. Document maintenance

Update this file when:
- A phase document from `docs/` is completed and changes something described here (roles, tenancy, auth, security posture)
- A locked decision in `docs/DECISIONS.md` changes or a new one is added
- The auth migration decision is made
- A UI phase (12–17) completes, or `docs/UI_SPEC.md` itself is amended (keep Section 9 pointing at current reality)
- The Coolify/Hetzner migration happens (update Section 4)

When updating, edit the relevant section directly rather than appending a changelog at the bottom — this file should always describe current reality, not a history of it. (History belongs in `docs/DECISIONS.md`, `docs/AUDIT_REPORT.md`, and git history.) Do update the "Last updated" line at the top each time.
