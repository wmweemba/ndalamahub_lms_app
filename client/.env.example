# Not needed in dev — the Vite dev proxy (vite.config.js) forwards '/api' to
# the local server so the session cookie stays same-origin. Set this only for
# a setup that isn't proxied (e.g. pointing a built client at a remote API).
VITE_API_URL=
